fastapi>=0.115.6
uvicorn[standard]>=0.34.0
supabase==2.13.0
httpx==0.28.1
pydantic>=2.11.7
pydantic-settings>=2.7.1
python-multipart>=0.0.26
python-dotenv>=1.1.1
lxml>=5.0.0
razorpay==2.0.1
openai>=1.0.0
scrapling[all]>=0.4.2

# Encryption at rest for stored API keys (app/services/token_crypto.py)
cryptography>=43.0.0
# Scheduler/autopilot use zoneinfo (Asia/Kolkata etc.); OS tzdata is not
# guaranteed on slim/Windows hosts, so ship the database with the app.
tzdata>=2024.1
